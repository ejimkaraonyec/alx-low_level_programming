#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: This program prints out the strings
 * passed to puts() to the standard output
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
